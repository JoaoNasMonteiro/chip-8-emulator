Maze, by David Winter

Drawing a random maze like this one consand lef-t-to-right
line. Each line is composed of a 4*4 bitmap. As the lines must form nonmaze is composed of 16 lines (as m mode. We choose a random value (0 or 1).
If it is 1,, we draw a left line bitmap. If it is 0, we draw a right one.
